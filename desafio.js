/* 
* Creat 2 JavaScript files:
* first one will export a fuction called getFlagValue()
* which will receive a string type argument
* this fuction should search inside the array, using process.argv the needed flag, which is the string
* and returns the value of the flag

* second one will import the fuction and pass the needed flag
*
* we will execute the second file on the terminal passing the flags --name and --greeting
* which will print on the terminal the greeting and the name of the person

*/